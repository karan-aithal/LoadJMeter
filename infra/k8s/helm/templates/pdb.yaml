# Prevent scale-down from killing the last worker pod when a job is in flight.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: worker-pdb
  namespace: {{ .Values.global.namespace }}
spec:
  maxUnavailable: 0
  selector:
    matchLabels:
      app: worker
